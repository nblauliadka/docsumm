export function MobileNav() {
  return (
    <nav className="md:hidden fixed bottom-0 left-0 right-0 h-16 border-t flex items-center justify-around safe-bottom" 
         style={{ borderColor: 'var(--border)', backgroundColor: 'var(--bg)' }}>
      <div className="text-xs" style={{ color: 'var(--text-2)' }}>
        Mobile Nav — Sprint 1 Placeholder
      </div>
    </nav>
  )
}
